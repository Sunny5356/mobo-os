name: mobo_app
description: MOBO mobile app (minimal scaffold)
publish_to: none
version: 0.1.0

environment:
  sdk: '>=2.18.0 <3.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  drift: ^2.7.0
  drift_flutter: ^2.7.0
  sqlite3_flutter_libs: ^0.5.7
  path_provider: ^2.0.0
  http: ^0.13.0
  fl_chart: ^0.60.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.0
  drift_dev: ^2.7.0

flutter:
  uses-material-design: true
