version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: mobo
      POSTGRES_PASSWORD: mobo_pass
      POSTGRES_DB: mobo
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
